# Lychee link checker — lenient configuration
# Only fail on real errors, not rate-limits or transient issues

# Accept common HTTP error codes (rate-limiting, timeouts, etc.)
accept = [404, 429, 500, 502, 503]
